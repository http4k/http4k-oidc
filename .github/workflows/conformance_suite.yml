name: Run OpenID Conformance Tests

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: CONFORMANCE_API_TOKEN=${{secrets.CONFORMANCE_API_TOKEN}} ./gradlew :test-coordinator:run